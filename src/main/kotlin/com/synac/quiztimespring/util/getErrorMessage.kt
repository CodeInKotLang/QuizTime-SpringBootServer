package com.synac.quiztimespring.util

/*
import io.ktor.http.*
import io.ktor.server.response.*
import io.ktor.server.routing.*

fun DataError.getErrorMessage(): String {
    return when(this) {
        DataError.Database -> "Database error occurred"
        DataError.NotFound -> "Resource not Found"
        DataError.Validation -> "Invalid data provided"
        DataError.Unknown -> {
            call.respond(
                message = ,
                status = HttpStatusCode.InternalServerError
            )
        }
    }
}*/
